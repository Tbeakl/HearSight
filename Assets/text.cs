using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;


public class text : MonoBehaviour
{
    public Text myText;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // Camera maincam = GameObject.Find("AR Camera").GetComponent<Camera>();
        // myText.text = maincam.transform.position.ToString("F3") + "\n" + maincam.transform.forward.ToString("F3");
    }
} 
